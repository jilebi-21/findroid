<resources>
    <!-- Player theme -->
    <style name="Theme.Findroid.Player" parent="Theme.Findroid">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Findroid" parent="Base.Theme.Findroid" />

    <style name="Base.Theme.Findroid" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorAccent">@color/accent</item>
        <item name="colorOnAccent">@color/on_accent</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>

        <item name="android:colorBackground">@color/surface</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="android:textColorPrimary">@color/text_color_primary</item>
        <item name="android:textColorSecondary">@color/text_color_secondary</item>
        <item name="textColorPrimary">@color/text_color_primary</item>
        <item name="textColorSecondary">@color/text_color_secondary</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- Shapes -->
        <item name="shapeAppearanceCornerExtraSmall">
            @style/ShapeAppearance.Findroid.Corner.ExtraSmall
        </item>
        <item name="shapeAppearanceCornerSmall">@style/ShapeAppearance.Findroid.Corner.Small</item>

        <!-- Toolbar -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar.Surface</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowLightNavigationBar">?attr/isLightTheme</item>

        <!-- Extra -->
        <item name="elevationOverlayEnabled">false</item>
        <item name="alertDialogTheme">@style/ThemeX.AlertDialogTheme</item>
        <item name="preferenceTheme">@style/ThemeOverlay.Findroid.Preference</item>
        <item name="bottomNavigationStyle">@style/ThemeX.BottomNavigationStyle</item>
        <item name="navigationRailStyle">@style/ThemeX.NavigationRailStyle</item>
        <item name="linearProgressIndicatorStyle">@style/ThemeX.LinearProgressIndicator</item>
        <item name="materialIconButtonFilledTonalStyle">@style/ThemeX.IconTonalButton</item>
        <item name="loadingProgressStyle">@style/ThemeX.LoadingProgressStyle</item>
    </style>

    <style name="ThemeOverlay.Findroid.DynamicColors" parent="ThemeOverlay.Material3.DynamicColors.DayNight">
        <item name="android:navigationBarColor">?attr/colorSurfaceContainer</item>
    </style>
</resources>